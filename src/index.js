import React from "react";
import ReactDOM from "react-dom";
import "./css/index.css";
import App from "./App";

// render it somewhere in the page
// get data for variables
// save everything inside of the localstorage

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
